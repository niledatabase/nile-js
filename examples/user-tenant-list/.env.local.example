# Client (public) env vars

# the URL of this example + where the api routes are located
NEXT_PUBLIC_BASE_PATH=http://localhost:3000/api

# Server env vars

# These DB creds can be generated by the admin dashboard for the database
NILE_USER=
NILE_PASSWORD=
NILE_WORKSPACE=
NILE_DATABASE=
