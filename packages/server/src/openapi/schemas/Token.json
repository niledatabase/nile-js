{
  "required": ["token", "type"],
  "type": "object",
  "properties": {
    "token": {
      "type": "string"
    },
    "maxAge": {
      "minimum": 0,
      "type": "integer",
      "format": "int32"
    },
    "type": {
      "type": "string",
      "enum": ["ACCESS_TOKEN", "REFRESH_TOKEN", "ID_TOKEN"]
    }
  }
}
