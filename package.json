{
  "name": "nile-js",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "workspaces": [
    "lib/nile",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @nile/js build && yarn workspace dashboard build"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.2.1",
    "@commitlint/config-conventional": "^16.2.1",
    "husky": "^7.0.4"
  }
}
